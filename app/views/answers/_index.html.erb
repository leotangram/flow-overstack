<div class="container">
  <h2>Answers</h2>
  <p>Aquí podrás ver el listado de las respuestas realizadas:</p>
  <table class="table">
    <thead class="thead-dark;" style="background-color: #FFBB33">
      <tr>
        <th>Votos</th>
        <th>Description</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
    <% @question.answers.each do |answer| %>
      <tr>
        <td>4</td>
        <td> <%= answer.description %></td>
        <td> <%= answer.user.email %></td>
      </tr>
      <tr>
        <td> <%= render '/comments/index', commentable: answer %></td> 
        <td> <%= render '/comments/form', commentable: answer %> </td> 
      </tr>
    <% end %>
    </tbody>
  </table>
</div>





